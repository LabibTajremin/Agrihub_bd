# Architecture

AgriSmart is a Go **modular monolith** (API-only) plus a Flutter mobile app.
Sections are filled in by the phase that introduces the behaviour.

## Layering
See `docs/agent/BUILD_INSTRUCTION.md` §3.
