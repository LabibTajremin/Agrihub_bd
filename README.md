# AgriSmart

Offline-first, voice-navigable agriculture app for smallholder rice farmers in Bangladesh.

- `backend/` — Go API (modular monolith, clean architecture)
- `mobile/` — Flutter app (Android primary, iOS parity)
- `docs/` — developer documentation; start with `docs/DEVELOPMENT.md`

```bash
make verify   # the full gate CI runs
```
