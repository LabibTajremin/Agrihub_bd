{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "api/index.go": { "maxDuration": 30 }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }],
  "build": {
    "env": { "GO_BUILD_FLAGS": "-tags serverless -ldflags '-s -w'" }
  }
}
